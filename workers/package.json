{
  "name": "loveveryfans-alerts",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for Loveveryfans promotion alerts and email subscriptions",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "db:init": "wrangler d1 execute loveveryfans-alerts-db --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.99.0"
  }
}
